<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".fragments.CommentFragment">

   <androidx.recyclerview.widget.RecyclerView
       android:layout_width="match_parent"
       android:layout_height="match_parent"
       android:id="@+id/commentRecyclerId"

       android:layout_above="@+id/commentFragLayout">

   </androidx.recyclerview.widget.RecyclerView>


    <LinearLayout

        android:padding="12dp"
        android:id="@+id/commentFragLayout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:orientation="horizontal"
        android:layout_alignParentBottom="true"
        android:layout_alignParentStart="true"
        android:layout_alignParentRight="true"

       >

        <EditText
            android:id="@+id/commentFragEditId"
            android:layout_width="340dp"
            android:layout_height="wrap_content"
            android:layout_margin="2dp"
            android:background="@drawable/edit_text_bg"
            android:elevation="2dp"
            android:hint="Write down your comment"
            android:padding="8dp" />

        <ImageView
            android:id="@+id/commentFragSendButtonId"
            android:layout_width="28dp"
            android:layout_height="28dp"
            android:layout_marginLeft="8dp"
            android:src="@drawable/icon_send" />

    </LinearLayout>

</RelativeLayout>